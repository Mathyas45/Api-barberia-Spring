# ============================================
# ARCHIVO DE EJEMPLO DE VARIABLES DE ENTORNO
# ============================================
#
# INSTRUCCIONES:
# 1. Copia este archivo y renómbralo a ".env"
# 2. Reemplaza los valores de ejemplo con tus datos reales
# 3. NUNCA subas el archivo .env a Git (debe estar en .gitignore)
#
# ============================================

# ===========================
# BASE DE DATOS
# ===========================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=barberia_db
DB_USERNAME=root
DB_PASSWORD=tu_password_mysql_aqui

# ===========================
# JWT SEGURIDAD
# ===========================
# ⚠️ CRÍTICO: Esta clave es OBLIGATORIA
# La aplicación NO arrancará sin ella
#
# GENERAR CLAVE SEGURA:
#
# PowerShell (Windows):
#   $bytes = New-Object byte[] 64
#   [Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($bytes)
#   [Convert]::ToBase64String($bytes)
#
# Bash/Linux/Mac:
#   openssl rand -base64 64
#
# Node.js:
#   node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
#
# ⚠️ REQUISITOS:
# - Mínimo 43 caracteres (256 bits)
# - Recomendado 64+ caracteres (512 bits)
# - NUNCA usar claves triviales como "secret" o "123456"
# - DIFERENTE para desarrollo y producción
#
JWT_SECRET_KEY=GENERA_TU_CLAVE_AQUI_CON_LOS_COMANDOS_DE_ARRIBA
JWT_EXPIRATION=86400000

# ===========================
# PERFILES
# ===========================
# Valores posibles: dev, prod
# dev  = Sin seguridad JWT (para testing rápido)
# prod = Con seguridad JWT completa
SPRING_PROFILES_ACTIVE=dev
